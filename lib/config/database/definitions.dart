enum DATABASE_DEFINE__ {
  DATABASE,
  DATABASE_NAME,
  DATABASE_HOST,
  DATABASE_VERSION,
  DATABASE_USER,
  DATABASE_PASSWORD,
}

enum DATABASE {
  GOOGLE_FIREBASE,
  POSTGRE_SQL,
  SQLITE,
  MONGO_DB,
}
